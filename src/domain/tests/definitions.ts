/** Transitional domain entrypoint; definitions remain data-only and are re-exported for compatibility. */
export {
  ALL_TESTS,
  FREQUENCY_4,
  STRESS_5,
  TESTS,
  TESTS_ASRS,
  TESTS_ISI,
  TESTS_MDQ,
  TESTS_PSS10,
  TESTS_WHO5,
  getTest,
} from "@/data/tests";
export type { TestDefinition, TestOption, TestScoring } from "./types";
